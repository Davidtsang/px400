<%= form_for(current_user.active_relationships.find_by(followed_id: user.id),
             html: { method: :delete },
             remote: true) do |f| %>
    <button class="btn btn-default" type="submit"><i class='fa fa-user-times'></i> 取消关注</button>
<% end %>